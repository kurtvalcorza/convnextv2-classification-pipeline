# Release status

Current status: **Candidate — default-path execution recorded on 2026-09-26 (Kaggle T4); REL12 BYOD exercise pending before promotion**. The pipeline package, the offline test suite (including a small random-weight `ConvNextV2ForImageClassification` run through prediction, the zero-shot baseline, full and frozen fine-tuning, evaluation and adapter reload, and a full-size random checkpoint taken through the strict loader), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/convnextv2_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `f4db009e63145e02b3c075aa64d90ce41bcca4b1` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `ac05396e9e8b…`, matching the Hub's LFS record). The exact commit `8356fef` / notebook blob `f7d1d0f11ae1` executed top-to-bottom on Kaggle Tesla T4 on 2026-09-26 UTC with both BYOD branches off (276.9 s, 14/14 post-restart code cells): on 60 held-out CIFAR-10 thumbnails the zero-shot ImageNet mapping and the fine-tuned head both scored accuracy 1.000 (majority baseline 0.500, untrained head 0.8167), so the fine-tune shows no measurable gain on this split, and the tutorial split is not aware of the sample archive's 100 pixel-identical groups. The full record is in `docs/release-verification.md`. Release step 7 (REL12: the BYOD dataset branch with a small class-folder directory and with an incompatible one) has not been exercised, so the status stays Candidate.
